{% if breadcrumb %}
  <div class="breadcrumbs">
    {% for item in breadcrumb %}
        {% if item.url %}
          <a href="{{ item.url }}">{{ item.text }}</a>
        {% else %}
        <span>{{ item.text }}</span>
        {% endif %}
      <div>|</div>
    {% endfor %}
  </div>
{% endif %}
